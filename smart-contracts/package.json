{
  "name": "nft-marketplace-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for NFT Marketplace",
  "main": "index.js",
  "scripts": {
    "deploy:localhost": "npx hardhat run deploy.js --network localhost",
    "deploy:polygon": "npx hardhat run deploy.js --network polygon",
    "deploy:mumbai": "npx hardhat run deploy.js --network mumbai",
    "verify": "npx hardhat verify --network polygon",
    "test": "npx hardhat test",
    "compile": "npx hardhat compile"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-etherscan": "^3.1.7",
    "@nomiclabs/hardhat-waffle": "^2.0.6",
    "chai": "^4.3.7",
    "ethers": "^5.7.2",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.6.1"
  }
}
